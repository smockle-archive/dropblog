{
  "name": "dropbox-to-sns",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "all": "npm run _install && npm run build && npm run package",
    "_install": "npm install -p .",
    "build": "tsc -p tsconfig.json",
    "package": "zip -r dropbox-to-sns *"
  },
  "author": "Clay Miller <clay@smockle.com> (https://www.smockle.com/)",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^11.13.0",
    "typescript": "^3.3.3"
  },
  "dependencies": {
    "aws-sdk": "^2.400.0"
  }
}
